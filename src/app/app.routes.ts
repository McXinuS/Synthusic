import {Routes} from "@angular/router";

export const ROUTES: Routes = [
  // TODO redirect to login window if not logged in
  // { path: '', redirectTo: 'login', pathMatch: 'full' },

  // TODO show 404 page
  // { path: '**', component: PageNotFoundComponent }
];
