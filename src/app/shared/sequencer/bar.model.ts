export class Bar {
  index: number;
  width: number;
  isFirst: boolean = false;
  isLast: boolean = false;

  constructor(index: number, width: number) {
    this.index = index;
    this.width = width;
  }
}
