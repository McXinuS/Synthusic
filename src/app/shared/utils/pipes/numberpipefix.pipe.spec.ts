import { NumberpipefixPipe } from './numberpipefix.pipe';

describe('NumberpipefixPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberpipefixPipe();
    expect(pipe).toBeTruthy();
  });
});
